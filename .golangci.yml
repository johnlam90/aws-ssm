run:
  timeout: 5m
  issues-exit-code: 1
  skip-dirs:
    - .history
linters-settings:
  gofmt:
    simplify: true
  govet:
    check-shadowing: true
linters:
  enable:
    - gofmt
    - govet
    - staticcheck
    - ineffassign
    - revive
    - errcheck
    - unused
    - typecheck
    - gosimple
    - stylecheck
    - gocritic
    - durationcheck
issues:
  exclude-rules:
    - path: _test\.go$
      linters:
        - errcheck
